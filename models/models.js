// const Post = require('../posts/model')
// const User = require('../users/model')
// User.belongsToMany(Post, {foreignKey: 'id', sourceKey: 'author'})
// Post.belongsToMany(User, {foreignKey: 'id', targetKey: 'author'})